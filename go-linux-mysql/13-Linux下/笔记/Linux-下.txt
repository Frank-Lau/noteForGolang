Linux系统中隐藏文件：

	ls -a : 所有以“.”开头的文件为隐藏文件。

	ls -h : 以人类可读语法显示。
	
chmod修改文件属性：

	文字设定法：
		u：所有者
		g：所属组
		o：其他人

		+：添加权限
		-：去除权限
		=：赋值程某个权限。

	数字设定法：【重点】

		r：4
		w：2
		x：1

ps：
	ps  xua | grep xxx

top:
	Linux 下的任务查看器。

kill：
	kill -9 PID（进程ID）  强制杀死指定进程

	kill -l 	查看当前系统使用的信号编号。


前台、后台： 以是否能与用户交互为判断依据。  & ： 将命令放到后台执行。    firefox www.baidu.com &

Ctrl+Alt +f3 : 切换为 文字界面

Ctrl+Alt +f1 : 切换为 图形界面 —— X window

ifconfig：
	查看当前主机网络状态。   获取IP地址。

	sudo ifconfig 网卡名（ens33） 新IP  	—— 临时设置IP地址。		

	永久设置IP：	1. 图形界面   --- 【重点】
		
			2. 修改 /etc/ 下相关配置文件。
ping ：
	测试当前主机和远程主机之间的网络连通状态。

find：--- 按文件属性  【重点】

	语法： find  搜索目录位置  参数  关键字。

	-name：按名字搜索
		find ./ -name "init*"

	-type:   按类型搜索
		find ./ -type "p"/"s"/"d"....

		普通文件： “f”

	-size： 按大小搜索	
		find ./ -size +3k -size -5k   （第2个-size不能省略）

		+：大于、-：小于

		k ：必须小写！

		M：必须大写！

		无单位：按扇区（512B）个数计算

	-maxdepth： 指定搜索层级

		指定搜索层级深度。应该防置于其他参数之前。

		find ./ -maxdepth 1 -type "d" -name "go*"

	扩展自学： ctime、atime、mtime、cmin、amin、mmin  按时间搜索

	-exec： 将搜索结果，统一执行某一命令

		语法： find 目录  参数 关键字  -exec   待执行命令  {} \;
				------------------------
		find ./ -maxdepth 1 -type "f" -exec  ls -lh {} \;

	-xargs:  将搜索结果，统一执行某一命令

		语法：  find 目录  参数 关键字 | xargs 待执行命令

		防置搜索到的目标中，含有特殊字符（如：空格） ，使用 -print0 来弥补。： find 目录  参数 关键字 -print0 | xargs -0 待执行命令

		find ./ -maxdepth 2 -type "f" -print0 | xargs -0 ls -lh

grep： --- 按文件内容搜。 【重点】

	语法： grep -r 搜索关键字 目录

		grep -r “致爱丽丝”./ -n (显示关键字在文件中的行号)	

find、xargs、grep 结合；

	find ./ -type "f" -print0 | xargs -0 grep -r "love" -n 

	find ./ -type "f" -exec  grep -r "love" -n {} \;

vim/vi 编辑器：  ——  文本编辑器。  vim 文件名

	命令模式： i：向光标前插入， 切换工作模式  【重点】

		a：向光标后插入， 切换工作模式

		o：向关标所在行的下一行插入，切换工作模式 【重点】

		O：向关标所在行的上一行插入，切换工作模式

		I：插入光标所在行的行首。切换工作模式

		A：插入光标所在行的行尾。切换工作模式

		s：以删除一个字符为条件，切换工作模式

		S：以删除光标所在行为条件，切换工作模式

	编辑模式： 写、删除、修改文本内容

	末行模式： “：”   末行命令

移动：
	h、j、k、l：左、下、上、右

删除、剪切、粘贴：

	x：删除一个光标选中字符

	dd： 删除(剪切)光标所在行

	ndd： n表示要删除（剪切）行数。 将光标挪移至首行上。

	dw：删除一个单词。将光标置于单词首字母。

	D/d$：从光标所在位置删除（剪切）到本行行尾。

	d0：从光标所在位置删除（剪切）到本行行首。

	区域删除（剪切）：将光标挪至待剪切的首字符上，按“v”（左下角出现“可视”提示）。使用hjkl挪移光标，选择操作区域。 按d剪切。 

	p（小写）：粘贴。  向光标所在行后粘贴。

	P（大写）：粘贴。  向光标所在行前粘贴。
	
撤销：
	u
反撤销：
	ctrl + r

复制：
	yy：复制光标所在行

	nyy： n表示复制的行数。 将光标挪移至首行上。

	yw：复制一个单词。将光标置于单词首字母。

	y$: 从光标所在位置复制到本行行尾。

	y0: 从光标所在位置复制到本行行首。

	区域复制：将光标挪至待剪切的首字符上，按“v”（左下角出现“可视”提示）。使用hjkl挪移光标，选择操作区域。 按y复制。

跳转：
	G：跳转文本末尾行

	gg：文本的第一行	

	gg=G： 自动排版。

	nG： n表示要跳转的行号

	末行命令：“：行号”回车
查找：
	“/”: 键入用户想象的待查询字符串。 n ，检索下一个。

	“*”：将光标放置于待查询单词任意字符上，使用*向后检索。使用“#”向前检索。

替换：
	“r”：单字符替换。 光标选中待替换字符，按“r”，输入要替换为的字符。

	替换一行中一个单词：

		末行下，:s/旧字符串/新字符串  —— 替换光标所在行的第一个。 要替换全部 加：/g

	通篇替换：
		末行下，:%s/旧字符串/新字符串  —— 替换本文所有行的第一。 要替换全部 加：/g

	指定起始、终止行替换：

		末行下，:起始行号，终止行号s/旧字符串/新字符串  —— 替换选定行号的每行的第一个。 要替换全部 加：/g
分屏显示：

	：vsp  垂直分屏。	在其后添加文件名，分屏打开多个文件。

	：sp   水平分屏。	在其后添加文件名，分屏打开多个文件。

	Ctrl + ww 再各个分屏中跳转。

	：wq 退出当前分屏。 ：wqall 退出所有。

在vim中使用 shell 命令：

	：！shell命令 

vim的配置文件：
	
	/etc/vim/vimrc

远程操作：
	Linux -- linux  【重点】

		sudo aptitude show ssh —— 测试当前linux主机是否安装 ssh

	远程连接：语法：ssh  目标主机用户名@目标主机IP地址 ，回车。 ——> 输入用户密码

	远程数据传输：	

		本地传远端：scp -r  待发送文件路径   目标主机用户名@目标主机IP地址：/存储位置绝对路径

		远端传本地：scp -r  目标主机用户名@目标主机IP地址：/待拷贝文件的绝对路径   本地存储目录位置

	windows -- linux：
	


	
	




	

	

		

	


















		

		